<a
    href="{{ path('app_home') }}"
    class="btn ef-auth__back border-0 shadow-sm bg-body"
    aria-label="{{ 'ui.auth.back_home_aria'|trans }}"
    title="{{ 'ui.auth.back_home_aria'|trans }}"
    data-turbo="false"
>
    <i class="bi bi-arrow-left" aria-hidden="true"></i>
</a>
