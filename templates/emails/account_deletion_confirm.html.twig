{% extends 'emails/_base.html.twig' %}

{% block email_title %}{{ 'email.account_deletion_confirm.title'|trans(locale=ef_email_locale) }}{% endblock %}

{% block email_body %}
    <p>{{ 'email.common.greeting'|trans({'%name%': user.displayName}, locale=ef_email_locale) }}</p>
    <p>{{ 'email.account_deletion_confirm.intro'|trans(locale=ef_email_locale)|raw }}</p>
    <p>{{ 'email.account_deletion_confirm.cta_intro'|trans({
        '%expires%': 'email.common.expires_hours'|trans({'%hours%': expiresHours}, locale=ef_email_locale),
        '%single_use%': 'email.common.single_use'|trans(locale=ef_email_locale),
    }, locale=ef_email_locale)|raw }}</p>
    <p style="margin: 2rem 0;">
        <a href="{{ confirmUrl }}"
           style="background: #c82333; color: #fff; padding: 12px 24px; text-decoration: none; border-radius: 8px; font-weight: bold;">
            {{ 'email.account_deletion_confirm.cta'|trans(locale=ef_email_locale) }}
        </a>
    </p>
    <p style="font-size: 0.9rem; color: #666;">
        {{ 'email.common.link_direct'|trans(locale=ef_email_locale) }}<br>
        <a href="{{ confirmUrl }}">{{ confirmUrl }}</a>
    </p>
    <p style="font-size: 0.85rem; color: #999;">{{ 'email.account_deletion_confirm.ignore'|trans(locale=ef_email_locale)|raw }}</p>
{% endblock %}
