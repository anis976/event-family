security:
    password_hashers:
        Symfony\Component\Security\Core\User\PasswordAuthenticatedUserInterface: 'auto'

    providers:
        app_user_provider:
            entity:
                class: App\Entity\User
                property: email

    role_hierarchy:
        ROLE_MODERATOR: ROLE_USER
        ROLE_SUPER_MODERATOR: ROLE_MODERATOR
        ROLE_ADMIN: [ROLE_SUPER_MODERATOR, ROLE_MODERATOR, ROLE_USER]

    firewalls:
        dev:
            pattern: ^/(_profiler|_wdt|assets|build|_error)/
            security: false
        main:
            lazy: true
            provider: app_user_provider
            user_checker: App\Security\UserChecker
            login_throttling:
                max_attempts: 5
                interval: '15 minutes'
            form_login:
                login_path: app_login
                check_path: app_login
                enable_csrf: true
                csrf_parameter: _csrf_token
                csrf_token_id: authenticate
                default_target_path: app_home
            remember_me:
                secret: '%kernel.secret%'
                lifetime: 604800
            logout:
                path: app_logout
                target: app_home
                enable_csrf: true
            custom_authenticators:
                - App\Security\GoogleAuthenticator

    access_control:
        - { path: ^/connect, roles: PUBLIC_ACCESS, requires_channel: '%env(string:REQUIRED_SCHEME)%' }
        - { path: ^/inscription/google/terminer, roles: ROLE_USER, requires_channel: '%env(string:REQUIRED_SCHEME)%' }
        - { path: ^/login, roles: PUBLIC_ACCESS, requires_channel: '%env(string:REQUIRED_SCHEME)%' }
        - { path: ^/mot-de-passe-oublie, roles: PUBLIC_ACCESS, requires_channel: '%env(string:REQUIRED_SCHEME)%' }
        - { path: ^/reinitialiser-mot-de-passe, roles: PUBLIC_ACCESS, requires_channel: '%env(string:REQUIRED_SCHEME)%' }
        - { path: ^/register, roles: PUBLIC_ACCESS, requires_channel: '%env(string:REQUIRED_SCHEME)%' }
        - { path: ^/verify-email, roles: PUBLIC_ACCESS, requires_channel: '%env(string:REQUIRED_SCHEME)%' }
        - { path: ^/profil/mot-de-passe/confirmer, roles: PUBLIC_ACCESS, requires_channel: '%env(string:REQUIRED_SCHEME)%' }
        - { path: ^/profil/suppression/confirmer, roles: PUBLIC_ACCESS, requires_channel: '%env(string:REQUIRED_SCHEME)%' }
        - { path: ^/verification/resend, roles: PUBLIC_ACCESS, requires_channel: '%env(string:REQUIRED_SCHEME)%' }
        - { path: ^/cgu, roles: PUBLIC_ACCESS, requires_channel: '%env(string:REQUIRED_SCHEME)%' }
        - { path: ^/mentions-legales, roles: PUBLIC_ACCESS, requires_channel: '%env(string:REQUIRED_SCHEME)%' }
        - { path: ^/about, roles: PUBLIC_ACCESS, requires_channel: '%env(string:REQUIRED_SCHEME)%' }
        - { path: ^/locale/switch, roles: PUBLIC_ACCESS, requires_channel: '%env(string:REQUIRED_SCHEME)%' }
        - { path: ^/$, roles: PUBLIC_ACCESS, requires_channel: '%env(string:REQUIRED_SCHEME)%' }
        - { path: ^%ef.admin.path%, roles: ROLE_MODERATOR, requires_channel: '%env(string:REQUIRED_SCHEME)%' }
        # ^/.+ et non ^/ : sinon "/" est exigé ROLE_USER avant la vitrine invitée (PUBLIC_ACCESS ^/$).
        - { path: ^/.+, roles: ROLE_USER, requires_channel: '%env(string:REQUIRED_SCHEME)%' }

when@test:
    security:
        password_hashers:
            Symfony\Component\Security\Core\User\PasswordAuthenticatedUserInterface:
                algorithm: auto
                cost: 4
                time_cost: 3
                memory_cost: 10
