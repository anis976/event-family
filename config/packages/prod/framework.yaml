# Résolution des assets compilés (asset-map:compile) en production.
# Filet de sécurité si le manifest AssetMapper n'est pas lu au runtime.
framework:
    assets:
        json_manifest_path: '%kernel.project_dir%/public/assets/manifest.json'
